<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@drawable/selector_gradient_bg</item>
    </style>

    <style name="TextBase">
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Onboarding -->
    <style name="OnBoardingTitle" parent="TextBase">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="OnBoardingText" parent="TextBase">
        <item name="android:textSize">18sp</item>
    </style>

    <!-- Poker card -->
    <style name="PokerCardBase">
        <item name="android:textColor">@color/dark</item>
        <item name="android:fontFamily">cursive</item>
    </style>

    <style name="PokerCardText" parent="PokerCardBase">
        <item name="android:textSize">120sp</item>
    </style>

</resources>
